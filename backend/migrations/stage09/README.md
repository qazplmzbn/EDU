# Stage 09 — Course-scoped profile state

Run after Stage 08. Back up all `student_*` profile tables. The migration assigns course ownership from canonical knowledge points and seeds the explainable `weighted_bkt_elo_v1` policy. Cleanup is deferred; rollback is additive.
