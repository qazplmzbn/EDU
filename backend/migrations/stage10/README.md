# Stage 10 — Versioned knowledge-only learning paths

Run after an ACTIVE Stage 08 graph and Stage 09 profile schema exist. Legacy paths become session + version 1; only knowledge items join the new version model. Resource/question/assignment items remain as compatibility rows until Stage 16 and are never returned by `/api/v1`.
