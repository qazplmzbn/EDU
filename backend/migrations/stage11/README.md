# Stage 11 — ResourceUnit and blueprint

Run after Stage 10. The schema is additive and immutable. ResourceUnit aggregation requires an ACTIVE Neo4j graph and an ACTIVE path version. Course capability seeds are defaults and may be disabled per course; policy thresholds are configuration, not literature constants.
