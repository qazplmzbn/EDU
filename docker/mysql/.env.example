# Copy to .env locally, then set a strong value. .env is ignored by Git.
MYSQL_ROOT_PASSWORD=replace-with-a-local-secret

# Avoid the host MySQL port while both services coexist.
MYSQL_HOST_PORT=3307
