# 后端系统


## 1. 文档目标


最终系统必须形成：

1. 学生选择课程和目标知识点。
2. 路径服务基于课程知识图谱、当前画像版本和策略版本生成只包含知识点的路径。
3. 资源服务把路径中连续且主题相连的知识点聚合为 `ResourceUnit`，生成教学资源、可见练习题和隐藏检测题。
4. 学生提交个性化资源生成习题的有效答案后，系统按生成题目标识校验并幂等写入交互记录，更新知识掌握度并生成新画像版本；其他题库、试卷和作业作答不进入该画像闭环。
5. 事件判断器检查连续答错、学习停滞、资源完成、目标变化、主动重规划和内容失效。
6. 只有事件成立且计算结果实际变化时才生成新路径版本；只有资源决策为生成或重生成时才创建新资源版本。
7. 所有画像、路径、资源和答题证据都可通过 `correlation_id`、版本号和不可变记录追溯。


## 2. 当前系统

### 2.1 当前技术与工程现状

- Java 17、Spring Boot 3.0.2、Spring Security、MyBatis、MySQL。
- 已有 `course`、`knowledge`、`profile`、`learning`、`bank`、`agent`、`source`、`dialogue`、`competency`、`llm` 等模块。
- `CourseLearningPathServiceImpl` 当前按 `course_knowledge.sequence_no` 生成路径，没有使用先修图拓扑排序；每次生成会把旧路径置为 `obsolete` 并直接增加版本。
- 当前 `learning_path_item` 同时允许 `knowledge/resource/question/assignment`，且路径生成会把学习资源作为路径节点插入，这与目标“路径只输出知识点”冲突。
- 当前知识点实体只有 `name/code/parentId/level/description` 等基础字段，缺少课程归属、知识类型、图版本和发布状态。知识点本身不设置难度，难度只属于个性化资源生成的具体习题。
- 当前知识关系只有 `sourceId/targetId/relationType/weight/confidence/sourceType`，缺少课程、图版本、发布批次和约束校验状态。
- 当前 `StudentKnowledgeState` 已有掌握度、置信度和证据计数的基础结构，但缺少课程维度、算法版本、最后处理交互序号和直接/间接证据审计。
- 当前 `StudentProfileSnapshot` 没有显式 `profileVersion/courseId/calculatedAt/algorithmVersion`，无法稳定复现路径与资源决策。
- 当前教师智能体资源生成已具备生成、审核和持久化雏形，但主要服务教师侧资源草案；没有按学生、路径版本、ResourceUnit、蓝图版本、发布版本组织完整生命周期。
- 当前作答流程围绕 `qb_attempt/qb_answer/qb_grading_record`，没有统一不可变 `resource_interaction`、事务 outbox、证据消费进度和画像更新日志。
- 当前 SQL 已按现有工程目录分布在 `backend/*.sql`、`backend/migrations/**` 等位置。本次保持既定目录结构，只补充脚本内容、执行说明和校验脚本。
- 当前知识关系主要保存在 MySQL 关系表中，尚未形成 Neo4j 图谱版本发布、同步、查询和一致性校验闭环。

### 2.2 目标状态

| 领域 | 当前状态 | 需求状态                                                                      |
|---|---|---------------------------------------------------------------------------|
| 路径内容 | 混合知识点和资源节点 | 路径版本仅含知识点步骤                                                               |
| 路径算法 | 课程配置顺序 | 先修闭包、掌握过滤、稳定拓扑排序、合法候选评分、局部重规划                                             |
| 图谱 | MySQL 关系表被动存储 | Neo4j 存储和查询课程知识图谱，MySQL 保存图版本、发布批次和业务关联                                   |
| 画像 | 局部状态表与快照 | 课程级五维画像、不可变版本、证据日志、处理进度                                                   |
| 答题闭环 | 作答与统计松散关联 | 仅接收个性化资源生成习题的作答 → interaction/outbox → 幂等画像更新 → 事件判断 → 路径/资源决策       |
| 资源 | 通用资源及教师生成草案 | ResourceUnit → Blueprint → Job → Bundle Version → Item → Review → Publish |
| 隐藏题 | 无统一隔离模型 | 数据库存储但学生接口绝不返回，达到检测时机后单独下发                                                |
| 版本策略 | 生成即加版本 | 内容实际变化才加路径版本；符合事件策略才加资源版本                                                 |
| SQL | 已按当前工程目录安排 | 保持现有目录不变，在原目录补充迁移、校验和执行说明                                         |

## 3. 总体架构与模块职责

保留现有 Spring Boot 单体作为业务主后端，在现有 `knowledge` 模块中补充 Neo4j 访问能力。MySQL 保存事务数据、图版本元数据、发布批次、同步记录和跨业务引用；Neo4j 保存已发布课程知识图谱的节点与关系并负责图遍历，两边通过 `course_id + graph_version` 对齐。

### 3.1 Java 主后端

- 身份、权限、课程、题库、作答、画像、路径、资源、Agent 编排、审计和事务一致性。
- 所有公开 REST API。
- 负责锁、幂等、版本持久化和 outbox。
- 通过知识模块中的 `KnowledgeGraphRepository/KnowledgeGraphService` 访问 Neo4j，不允许 Controller 直接执行 Cypher。

只在当前已有模块目录内新增以下类，不创建新的顶层模块、不移动现有文件：

```text
modules/profile/service/KnowledgeStateService.java
modules/profile/service/ProfileAggregationService.java
modules/profile/service/ProfileQueryService.java
modules/course/controller/LearningPathSessionController.java
modules/course/service/PathPlanningService.java
modules/course/service/PathEventDetector.java
modules/course/service/PathRefreshApplicationService.java
modules/agent/controller/PersonalizedResourceController.java
modules/agent/service/ResourceUnitService.java
modules/agent/service/ResourceBlueprintService.java
modules/agent/service/ResourceGenerationWorkflow.java
modules/agent/service/ResourceReusePolicy.java
modules/learning/controller/ResourceInteractionController.java
modules/learning/service/InteractionSubmissionService.java
modules/learning/service/OutboxPublisher.java
modules/knowledge/repository/Neo4jKnowledgeGraphRepository.java
modules/knowledge/service/KnowledgeGraphService.java
```

现有 `modules/course` 继续负责课程目录和课程知识范围，但不再承担个性路径核心算法。现有 `modules/agent` 的通用任务/步骤/审核能力可复用到资源生成工作流。

### 3.2 Neo4j 知识图谱存储与访问

Neo4j 使用课程级、版本化图模型。知识点节点：

```text
(:KnowledgePoint {
  knowledgePointId: 301,
  courseId: 12,
  graphVersion: "graph_course12_20260829_01",
  code: "kp_bplus",
  name: "B+树",
  knowledgeType: "CONCEPT",
  status: "ACTIVE"
})

(:KnowledgePoint)-[:PREREQUISITE {
  courseId: 12,
  graphVersion: "graph_course12_20260829_01",
  weight: 1.0,
  confidence: 0.96,
  sourceType: "TEACHER_CONFIRMED"
}]->(:KnowledgePoint)
```

关系类型固定为 `PREREQUISITE/SIMILAR/PART_OF/RELATED_TO/SUPPORTS`。`PREREQUISITE` 方向统一为“先修知识点 → 后继知识点”。

Neo4j 约束和索引至少包括：

```cypher
CREATE CONSTRAINT kp_version_identity IF NOT EXISTS
FOR (n:KnowledgePoint)
REQUIRE (n.courseId, n.graphVersion, n.knowledgePointId) IS UNIQUE;

CREATE INDEX kp_course_version IF NOT EXISTS
FOR (n:KnowledgePoint) ON (n.courseId, n.graphVersion);
```

Java 使用官方 Neo4j Java Driver，在现有 `modules/knowledge` 中实现先修闭包、有限跳邻居、目标可达性、环检测、子图读取和 ResourceUnit 主题连续性查询。复杂排序仍由 Java `PathPlanningService` 使用确定性算法完成，Neo4j 负责返回受 `course_id + graph_version` 约束的图数据。

图版本发布流程：

1. MySQL 创建 `knowledge_graph_version=DRAFT` 和同步任务。
2. 按 `course_id + graph_version` 将确认后的节点、关系写入 Neo4j。
3. 在 Neo4j 校验节点数、边数、跨课程关系、自环、重复关系和先修环。
4. 将校验报告、节点数、边数和内容哈希回写 MySQL。
5. 校验通过后，在 MySQL 事务中把旧 ACTIVE 改为 ARCHIVED、新版本改为 ACTIVE。
6. 失败时只清除该 Neo4j 草稿版本，不影响当前 ACTIVE 版本。

Neo4j 不可用时，只允许读取 MySQL 中已经保存的 ACTIVE 路径版本继续学习；禁止生成新路径或凭提示词猜测图关系，返回 `KNOWLEDGE_GRAPH_UNAVAILABLE` 并记录告警。

## 4. 数据库资源目录与迁移规则

### 4.1 资源放置

数据库文件放置到resource/sql里面，做好分类



每个现有迁移阶段补充同目录 `README.md` 或在已有说明中写明：MySQL 目标版本、Neo4j 目标版本、执行先后、备份要求、验证 SQL/Cypher、可回滚边界。项目当前没有 Flyway 依赖，本次不额外引入目录和命名体系。

### 4.2 通用字段规范

- 数据库物理主键继续使用 `BIGINT UNSIGNED`。
- 对外业务 ID 新增 `VARCHAR(64)`，例如 `student_code/path_code/resource_bundle_code/interaction_code`；禁止对外暴露可推断的连续主键。
- 时间统一 `DATETIME(3)`，Java 使用 `LocalDateTime`，接口使用带时区 ISO-8601。
- 比例/置信度使用 `DECIMAL(6,4)`，范围加 `CHECK (value >= 0 AND value <= 1)`。
- JSON 使用 MySQL `JSON`，不再用 `LONGTEXT` 保存需要查询或校验的结构；大段生成内容可用 `LONGTEXT`。
- 版本记录不可物理覆盖；状态切换通过事务完成。
- 所有跨模块表增加 `course_id`、`correlation_id`（适用时）、`created_at`。
- 逻辑删除只用于可维护字典；交互、版本、审核、日志、outbox 不允许逻辑删除。

## 5. 实体与字段改造总表

以下列出的“删除”是目标模型不再使用的字段。

### 5.1 知识点 `qb_knowledge_point`

保留：`id/name/code/parent_id/level/description/created_at/updated_at/is_deleted`。

新增：

| 字段 | 类型 | 示例 | 说明 |
|---|---|---|---|
| `course_id` | BIGINT UNSIGNED NOT NULL | `12` | 知识点必须归属课程 |
| `knowledge_type` | VARCHAR(32) NOT NULL | `CONCEPT` | CONCEPT/PROCEDURE/FACT/PRINCIPLE/SKILL |
| `status` | VARCHAR(24) NOT NULL | `ACTIVE` | DRAFT/ACTIVE/DISABLED |
| `content_version` | VARCHAR(64) NOT NULL | `kp_20260829_01` | 内容版本 |
| `metadata_json` | JSON NULL | `{"aliases":["B+ Tree"]}` | 扩展属性 |

删除：历史 `tag_id`（若任何旧脚本仍有该列）；如果已有草稿曾增加知识点 `difficulty`，一并删除且不迁移。禁止再用标签代表知识语义。增加唯一键 `(course_id, code)`。

### 5.2 知识关系 `qb_knowledge_relation`

将 `source_id/target_id` 重命名为语义明确的 `source_knowledge_point_id/target_knowledge_point_id`。对于 `PREREQUISITE`，方向固定为“先修 → 后继”。

新增：`course_id`、`graph_version_id`、`relation_code`、`status`、`published_at`、`created_by`。`relation_type` 枚举限定为 `PREREQUISITE/SIMILAR/PART_OF/RELATED_TO/SUPPORTS`。唯一键 `(graph_version_id, source_knowledge_point_id, target_knowledge_point_id, relation_type)`。

新增 `knowledge_graph_version`：

| 字段 | 示例 |
|---|---|
| `version_code` | `graph_course12_20260829_01` |
| `course_id` | `12` |
| `status` | `DRAFT/VALIDATING/ACTIVE/REJECTED/ARCHIVED` |
| `node_count/edge_count` | `86/142` |
| `content_hash` | SHA-256 |
| `validation_report_json` | DAG、重复边、孤立点报告 |
| `activated_at` | 发布时间 |

同一课程只允许一个 ACTIVE 图版本。

### 5.3 题目与知识关系

保留 `qb_question`、`qb_question_option` 等通用题库实体，但本闭环的画像更新不读取普通题库、试卷、作业或教师手工题的作答记录。只有个性化资源工作流生成并发布的练习题/隐藏检测题才能成为画像知识状态证据。

个性化资源题目仍使用统一题目标识，并使用 `question_knowledge` 建立知识绑定：

`id/question_id/knowledge_point_id/relation_type/weight/is_primary/cognitive_level/created_at`。

字段示例：`relation_type=DIRECT_ASSESSMENT`、`weight=0.7000`、`is_primary=1`、`cognitive_level=APPLY`。每题必须且只能有一个主知识点，全部权重之和应为 1（允许配置误差 `0.0001`）。

生成题目标识建议使用不可变 `generated_question_code`，示例 `pq_rb101_v8_0003`。该标识必须唯一绑定 `resource_bundle_id + resource_version + resource_item_id + question_difficulty + cognitive_level + grading_version`。难度不是知识点属性，也不由学生提交；它在 Blueprint/Generation 阶段为该生成题目标定，审核通过后随题目标识冻结。

`qb_attempt_question.tag_ids_json` 删除，新增 `knowledge_snapshot_json`，示例：

```json
{"primaryKnowledgePointId":301,"weights":{"301":0.7,"302":0.3},"graphVersion":"graph_course12_20260829_01"}
```

### 5.4 学生知识状态 `student_knowledge_state`

保留：`id/user_id/knowledge_point_id/mastery_value/confidence/evidence_count/correct_count/attempt_count/last_evidence_at/updated_at`。

新增：

- `course_id BIGINT UNSIGNED NOT NULL`，示例 `12`。
- `state_version BIGINT UNSIGNED NOT NULL`，示例 `19`。
- `calculation_method VARCHAR(64)`，示例 `DIMKT_ONLINE`。
- `algorithm_version VARCHAR(64)`，示例 `dimkt_mastery_v3`。
- `last_interaction_seq BIGINT UNSIGNED NOT NULL DEFAULT 0`。
- `last_interaction_id BIGINT UNSIGNED NULL`。
- `mastery_level VARCHAR(24)` 保留但只能由 mastery 映射得到，不能反向覆盖数值。

唯一键改为 `(user_id, course_id, knowledge_point_id)`。

### 5.5 五维画像实体

将当前泛化的 preference/ability 结构调整为可计算、可审计的五维状态：

1. `student_resource_preference`：`user_id/course_id/resource_type/score/confidence/evidence_count/calculated_at/algorithm_version`。示例 `resource_type=DIAGRAM, score=0.8200`。
2. `student_cognitive_state`：`user_id/course_id/cognitive_level/score/confidence/evidence_count/calculated_at/algorithm_version`。示例 `cognitive_level=APPLY, score=0.4300`。
3. `student_behavior_metric`：`user_id/course_id/metric_group/metric_code/value/confidence/evidence_count/calculated_at/algorithm_version`。主动性指标为 `EXTRA_LEARNING_RATE/HELP_SEEKING_RATE/ERROR_RECOVERY_RATE`；规律性为 `ACTIVE_DAY_RATE/ACTIVE_WEEK_RATE/WEEKLY_TIME_CV/REGULARITY_SCORE`。
4. `student_profile_snapshot`：新增 `course_id/profile_version/calculated_at/algorithm_version/correlation_id/knowledge_state_json/resource_preference_json/cognitive_profile_json/initiative_json/regularity_json`。
5. `knowledge_state_update_log`：记录 `user_id/course_id/knowledge_point_id/interaction_id/evidence_scope/previous_mastery/new_mastery/previous_confidence/new_confidence/model_version/profile_version_before/profile_version_after/created_at`；`evidence_scope` 为 `DIRECT/INDIRECT`。

删除或停止业务使用：`student_learning_preference.preference_value` 这种只保存文本分类、没有分数和置信度的主决策方式；`student_profile_snapshot.basic_state_json/skill_state_json/category_stat_json` 不进入本闭环，可迁移到扩展画像表。

### 5.6 学习路径

将 `learning_path` 作为稳定会话主表，将版本内容拆到 `learning_path_version`；不要继续把一行同时当会话和版本。

`learning_path`：

`id/path_code/user_id/course_id/target_knowledge_point_id/status/current_version/created_at/updated_at`。

`learning_path_version`：

`id/path_id/version/path_mode/based_on_profile_version/graph_version/policy_version/model_version/status/change_reason/correlation_id/created_at`。

示例：`path_mode=ADAPTIVE_UPDATED`、`version=4`、`status=ACTIVE`、`change_reason=CONSECUTIVE_WRONG`。

`learning_path_item` 改为路径版本步骤：

- 新增 `path_version_id`、`path_step_code`（示例 `ps_00009`）、`stage`、`reason_code`、`mastery_before`、`confidence_before`、`completed_at`。
- 保留 `order_no/knowledge_point_id/status/decision_reason`。
- 删除 `item_type/resource_id/question_id/assignment_id`。路径不得保存资源或题目节点。
- 唯一键 `(path_version_id, order_no)` 和 `(path_version_id, knowledge_point_id)`。

新增 `learning_path_progress`：`path_id/last_processed_interaction_seq/last_processed_interaction_id/consecutive_wrong_count/window_attempt_count/window_mastery_start/updated_at`，刷新时 `SELECT ... FOR UPDATE`。

新增 `path_update_log`：`path_id/old_version/new_version/trigger_event_types_json/trigger_interaction_ids_json/affected_knowledge_point_ids_json/added_step_ids_json/removed_step_ids_json/retained_step_ids_json/correlation_id/created_at`。

当前 `QbLearningPathSnapshot` 仅作历史迁移源，迁移完成后停止写入；不能误当画像快照。

### 5.7 个性化资源

现有 `qb_learning_resource` 可保留为资源内容基础表，但删除/停止使用单挂 `knowledge_point_id/tag_id`；知识覆盖统一使用关系表。

新增实体：

- `resource_unit`：`resource_unit_code/course_id/path_id/path_version_id/status/aggregation_evidence_json/created_at`。
- `resource_unit_step`：`resource_unit_id/path_step_id/order_no`，保证路径步骤连续且同版本只消费一次。
- `resource_blueprint`：`blueprint_code/resource_unit_id/profile_version_used/policy_version/status/resource_plan_json/learning_question_plan_json/hidden_assessment_plan_json/created_at`。
- `resource_generation_job`：`job_code/user_id/course_id/path_version_id/resource_unit_id/blueprint_id/idempotency_key/status/retry_count/max_revision_rounds/input_snapshot_json/error_code/error_message/correlation_id/created_at/started_at/finished_at`。
- `resource_bundle`：`bundle_code/user_id/course_id/resource_unit_id/blueprint_id/version/status/profile_version_used/graph_version/policy_version/content_hash/published_at/stale_reason/created_at`。
- `resource_item`：`bundle_id/item_code/generated_question_code/item_type/purpose/visibility/title/content_json/question_difficulty/cognitive_level/grading_key_json/order_no/status`。其中 `question_difficulty` 只对生成习题/隐藏检测题必填，随 `generated_question_code` 和资源版本冻结；普通讲解、图示、视频等资源项为空。
- `resource_knowledge`：`resource_item_id/knowledge_point_id/relation_type/coverage_weight/is_primary`。
- `resource_source`：`resource_item_id/source_chunk_id/evidence_type/citation_text/support_score`。
- `resource_review`：复用或扩展 `agent_review`，新增 `bundle_id/blueprint_id/review_dimension/repair_target/repair_scope/repair_action/repair_instruction/round_no`。

`visibility` 只能为 `VISIBLE/HIDDEN_UNTIL_ASSESSMENT/INTERNAL`；学生资源详情查询必须在 SQL 和 DTO 两层排除后两者及 `grading_key_json`。

### 5.8 交互、证据与事件

新增 `resource_interaction`。该表只记录个性化资源生成题目的最终作答，不记录资源打开、停留、跳过、普通题库作答、试卷作答或作业作答；非答题行为继续进入现有 `qb_learning_behavior`，仅用于资源偏好、主动性和规律性计算：

`id/interaction_code/interaction_seq/user_id/course_id/resource_bundle_id/resource_version/resource_unit_id/resource_item_id/generated_question_code/question_purpose/visibility/question_difficulty/primary_knowledge_point_id/knowledge_point_weights_json/score_normalized/correct/status/action_origin/grading_version/request_id/correlation_id/submitted_at/created_at`。

数据库不信任客户端提交的 `correct/question_difficulty/cognitive_level/knowledge_point_ids/grading_key`。提交时只接收 `generated_question_code` 和学生答案，服务端必须根据该标识定位唯一的已发布 `resource_item`，再读取冻结的难度、认知层级、知识点权重、评分规则和资源版本。无法定位、版本不匹配、不是生成习题或未发布时拒绝写入画像证据。

唯一键：`interaction_code`、`request_id`；顺序键：`(user_id, course_id, interaction_seq)`。

新增 `profile_evidence_event`：`event_id/interaction_id/consumer_name/status/retry_count/processed_at/error_message`，唯一键 `(event_id, consumer_name)`。

新增 `outbox_event`：`event_id/aggregate_type/aggregate_id/event_type/payload_json/status/retry_count/next_retry_at/correlation_id/created_at/published_at`。业务提交与 outbox 插入必须同事务。

## 6. 业务一：课程目录与知识图谱建设

### 6.1 数据库与实体

使用 `course`、`chapter`、`course_knowledge`、`qb_knowledge_point`、`knowledge_graph_version`、`qb_knowledge_relation`。若当前没有正式 `chapter` 表，新增 `course_chapter(id, course_id, chapter_code, chapter_name, order_no, status)`，知识点增加 `chapter_id`。

### 6.2 接口

- `GET /api/v1/courses/{courseId}/catalog`：课程、章节、知识点基础目录。
- `GET /api/v1/courses/{courseId}/chapters/{chapterId}`：章节知识点及学生掌握摘要。
- `POST /api/v1/admin/courses/{courseId}/graph-versions`：创建草稿图版本。
- `PUT /api/v1/admin/graph-versions/{version}/relations`：批量覆盖草稿边。
- `POST /api/v1/admin/graph-versions/{version}/validate`：在 Neo4j 中校验指定课程图版本。
- `POST /api/v1/admin/graph-versions/{version}/publish`：仅校验通过可发布。

### 6.3 持久化层

新增 `KnowledgeGraphVersionMapper`、`KnowledgeRelationMapper` 的按版本查询、批量插入、激活和锁定方法；在现有知识模块补充 `Neo4jKnowledgeGraphRepository`。发布事务锁定课程图版本，旧 ACTIVE 改为 ARCHIVED，新版本改 ACTIVE，写 outbox `knowledge.graph.published`。同步任务记录 Neo4j 节点/边数量、内容哈希和校验报告。

### 6.4 业务算法实现

人工维护：管理员录入知识点和关系，发布前严格校验。


校验规则：节点均属于课程；PREREQUISITE 无自环、无重复、整体为 DAG；边权 0~1；PART_OF 层级不能反向；禁用节点不得进入发布图；每条抽取关系至少关联一个 `source_chunk` 证据。图版本内容计算 SHA-256，内容未变不得创建空版本。

## 7. 业务二：学生画像更新与查询

### 7.1 数据库与实体

使用第 5.4、5.5、5.8 节实体。`user_id + course_id` 是画像版本边界；同一学生不同课程不得混用掌握度。知识状态更新只消费 `resource_interaction` 中来源合法的个性化生成题作答，普通题库、试卷、作业、人工录入成绩和资源浏览行为不得进入该更新接口。

### 7.2 接口

- `GET /api/v1/students/me/profile-summary?courseId=12`：学生可见摘要。
- `GET /internal/v1/profiles/{userId}/courses/{courseId}/knowledge-states?ids=301,302`。
- `GET /internal/v1/profiles/{userId}/courses/{courseId}/resource-preferences`。
- `GET /internal/v1/profiles/{userId}/courses/{courseId}/cognitive-profile`。
- `POST /internal/v1/profiles/interactions/{interactionId}/apply`：内部幂等更新。
- `POST /internal/v1/profiles/{userId}/courses/{courseId}/recalibrate`：离线全历史校准。

### 7.3 持久化层

`StudentKnowledgeStateMapper` 增加 `selectForUpdate`、`batchUpsert`；`StudentProfileSnapshotMapper` 增加 `selectLatestForUpdate` 和按 `profile_version` 查询。新增更新日志、行为指标、认知状态、偏好和证据事件 Mapper。

### 7.4 业务算法实现

`KnowledgeStateService` 必须定义：

```java
KnowledgeUpdateResult update(KnowledgeState previous, List<ValidatedInteraction> interactions);
KnowledgeState recalibrate(List<ValidatedInteraction> fullHistory);
```

第一版先上线可解释的加权 BKT/Elo 更新，后续接 DIMKT，但接口和输出保持一致。建议初版：

```text
evidenceWeight = questionWeight × purposeWeight × gradingConfidence
difficultyFactor = 1 + 0.5 × (generatedQuestionDifficulty - 0.5)
delta = learningRate × evidenceWeight × (scoreNormalized - masteryBefore) × difficultyFactor
masteryAfter = clamp(masteryBefore + delta, 0, 1)
confidenceAfter = 1 - exp(-evidenceCountAfter / confidenceScale)
```

其中 `generatedQuestionDifficulty` 必须从 `generated_question_code` 对应的已发布 `resource_item.question_difficulty` 读取，不从知识点读取，也不接受客户端传入。若历史生成题没有冻结难度，则该记录标记 `EVIDENCE_METADATA_MISSING`，不得更新掌握度。

隐藏检测题 `purposeWeight=1.0`，学习练习默认 `0.7`；具体参数放 `profile_algorithm_policy` 表，不写死在提示词。题目主知识点和权重指向的是 DIRECT 证据；模型对其他知识点的联动输出必须记为 INDIRECT。

DIMKT 接入要求：Python 模型中心提供推理接口，显式增加 `mastery_head` 输出统一知识点索引向量；Java 保存上次隐藏状态引用并只处理新交互。每日/每周全历史 `recalibrate` 校准。现有 HGNN 仅作为特征编码原型，不得宣称已经完成超图消息传播。

资源偏好至少累计打开、有效停留、完成、跳过、显式反馈后更新；单次点击不能产生高置信偏好。认知层级按资源蓝图预先确定的 Bloom 层级聚合。主动性必须依据 `action_origin` 排除 `SYSTEM_REQUIRED`。规律性只统计有效学习时长，不统计待机。

## 8. 业务三：个性学习路径

### 8.1 数据库与实体

使用 `learning_path`、`learning_path_version`、`learning_path_item`、`learning_path_progress`、`path_update_log` 和图/画像版本。

### 8.2 接口

- `POST /api/v1/learning-path-sessions`：输入 `courseId/targetKnowledgePointId`，支持 `Idempotency-Key`。
- `GET /api/v1/learning-path-sessions/{pathCode}`：返回当前 ACTIVE 版本。
- `POST /api/v1/learning-path-sessions/{pathCode}/refresh`：主动评估。
- `POST /internal/v1/learning-path-sessions/{pathCode}/evaluate-event`：画像更新后内部调用。

响应步骤只含 `pathStepId/orderNo/knowledgePointId/knowledgePointName/stage/status/reasonCode/decisionReason`，不含题目或资源。

### 8.3 持久化层

新增 `selectActiveVersionForUpdate`、`insertVersion`、`batchInsertSteps`、`markVersionSuperseded`、`insertUpdateLog`。只有新步骤序列或步骤状态实际变化才插入新版本。已完成前缀不可变。

### 8.4 基线路径算法

1. 从目标知识点沿 PREREQUISITE 入边反向遍历取得先修闭包。
2. 加入目标本身。
3. 过滤同时满足 `mastery >= masteryThreshold` 且 `confidence >= confidenceThreshold` 的节点，但目标节点默认保留。
4. 对剩余诱导子图做稳定拓扑排序。
5. 同层候选按策略评分，最后用 `knowledge_point_id` 作为稳定平局项。

```text
score(v) = 0.30 goalRelevance
         + 0.25 prerequisiteUrgency
         + 0.20 (1 - mastery)
         + 0.15 expectedLearningGain
         + 0.10 confidence
         - 0.10 recentRepetition
```

权重、掌握阈值、置信阈值、最大路径长度和跳数全部存 `path_policy`，并记录 `policy_version`。

### 8.5 事件与局部重规划

事件规则默认值：

- `CONSECUTIVE_WRONG`：同知识点连续错 3 次。
- `LEARNING_STAGNATION`：最近 3 次有效尝试掌握增益小于 0.01。
- `RESOURCE_COMPLETED`：完成当前资源规定学习项。
- `TARGET_CHANGED`：目标知识点改变。
- `MANUAL_REPLAN`：学生主动请求。
- `CONTENT_INVALIDATED`：图、课程内容或资源失效。

直接证据知识点为 affected 集合；加入其先修与后继最多 2 跳；阻塞时再加相似节点 1 跳。保留已完成前缀，仅重排未完成后缀。候选结果经过“课程范围、节点启用、先修顺序、无重复、最大长度”校验后与旧路径做规范化比较。完全相同则返回 `ACTIVE_UNCHANGED/EVENT_NO_CHANGE`，不加版本。

P-Agent 第一版就是确定性评分器的封装；未来 PPO 只能替换合法候选排序。S-Agent 只在停滞/同类错误达到阈值时选有限长度相似补救节点，并必须有退出条件。D-Agent 的题目生成职责全部移出路径域。

## 9. 业务四：ResourceUnit 与个性化资源生成

### 9.1 数据库与实体

使用第 5.7 节全部表，并复用 `agent_task/agent_step/agent_review/agent_decision/qb_llm_call` 记录执行轨迹。

### 9.2 接口

- `POST /api/v1/personalized-resources/jobs`：按当前路径步骤创建或复用任务。
- `GET /api/v1/personalized-resources/jobs/{jobCode}`：任务状态。
- `GET /api/v1/personalized-resources/{bundleCode}`：学生可见已发布资源。
- `POST /api/v1/personalized-resources/{bundleCode}/regenerate`：主动重生成。
- `POST /internal/v1/personalized-resources/decide`：路径/画像事件后的资源动作判断。

### 9.3 持久化层

任务创建唯一键建议为 `(user_id, path_version_id, resource_unit_id, generation_policy_version, idempotency_key)`。发布时锁定 bundle，计算内容哈希，只有监督通过才从 `IN_REVIEW` 转 `PUBLISHED`。旧版本根据业务置为 `SUPERSEDED` 或 `STALE`，不得覆盖。

### 9.4 ResourceUnit 聚合算法

从当前 LEARNING 步骤开始顺序扫描：只聚合同课程、同路径版本、连续 orderNo 的知识点；通过 Neo4j 查询 `PREREQUISITE/PART_OF/RELATED_TO/SUPPORTS` 检查主题连续性；遇到主题断裂、章节切换即结束。唯一约束保证同一路径版本的一个步骤只属于一个单元。

### 9.5 蓝图、生成、监督与修复

固定职责：

1. Resource Manager：校验上下文、聚合单元、编排状态；不计算画像、不规划路径。
2. Blueprint Agent：唯一可调用画像查询的教学设计者，输出资源槽位、支架等级、可见题计划、隐藏检测计划和 `profile_version_used`。
3. Generation Agent：只按已保存蓝图生成，不自行更换资源形式、难度或认知层级。
4. Supervision Group：分别检查事实证据、知识覆盖、路径对齐、难度适配、题目有效性、隐藏题独立性。

所有 Agent 输出用 JSON Schema 校验。审核 `REVISE` 必须返回 `repairTarget/repairScope/repairAction/repairInstruction`。最多 5 轮，超过后 job 为 FAILED。事实性内容必须关联 `source_chunk`；无充分证据返回 `EVIDENCE_INSUFFICIENT`，冲突返回 `EVIDENCE_CONFLICT`。

资源复用策略：普通掌握度数值变化继续复用；推荐知识点变化、资源完成、连续答错/停滞需补救、审核失败、主动重生成、资源或课程内容失效时才生成新版本。

## 10. 业务五：资源使用、答题提交与闭环事务

### 10.1 接口

- `POST /api/v1/resource-interactions`：只提交个性化资源生成习题的最终作答。
- `POST /api/v1/resource-interactions/{interactionCode}/submit`：如保留草稿模式，提交后不可变。
- `GET /api/v1/resource-interactions/{interactionCode}/result`：返回学生可见判分和后续动作。

请求只接受 `generatedQuestionCode/answer/actionOrigin/clientOccurredAt` 和幂等键。后端根据 `generatedQuestionCode` 反查唯一的 `resourceBundleCode/resourceVersion/resourceItemId`，并获取该生成题已冻结的难度、认知层级、知识点权重和 grading key。资源打开、停留、完成、跳过与反馈走现有学习行为接口，不写入 `resource_interaction`。

### 10.2 提交事务

`InteractionSubmissionService` 在一个事务内：

1. 校验 `Idempotency-Key/request_id`，重复请求直接返回原结果。
2. 通过 `generated_question_code` 锁定已发布 bundle/item，校验学生归属、资源版本、题目用途和可见时机；题目必须由个性化资源任务生成。
3. 从题目标识绑定记录读取 `question_difficulty/cognitive_level/knowledge_point_weights/grading_version`，服务端判分，生成 `interaction_seq` 和不可变 interaction。
4. 写 `outbox_event(eventType=learning.interaction.submitted)`。
5. 提交事务。

画像与路径闭环可采用同步应用服务加 outbox 兜底，或异步消费；无论哪种都必须以 `profile_evidence_event` 幂等。竞赛演示建议同步完成画像和路径决策，再由 outbox 做可靠通知。

### 10.3 画像与路径事务

1. 锁定 `learning_path_progress`。
2. 若 `interaction_seq <= last_processed_interaction_seq` 或 interaction 已消费，则返回既有处理结果。
3. 锁定相关 `student_knowledge_state`，更新掌握度，写 update log。
4. 保存新的 `student_profile_snapshot.profile_version`。
5. 运行事件判断。
6. 若需路径评估，锁定 ACTIVE path version，局部重规划；结果变化才保存新版本和 diff。
7. 运行资源策略，只写 `REUSE/GENERATE/REGENERATE/MARK_STALE` 决策；生成任务可事务后执行。
8. 原子推进 `last_processed_interaction_seq/id` 并提交。

任何一步失败全部回滚。返回：

```json
{
  "interactionId":"int_9001",
  "profileVersion":19,
  "masteryUpdated":true,
  "pathEvaluated":false,
  "pathChanged":false,
  "pathVersion":3,
  "resourceAction":"REUSE",
  "reason":"NO_PATH_EVENT"
}
```

## 11. 业务六：课程进度、考试资格与学习报告

课程完成度按路径中的课程核心知识点完成情况计算，不再把资源节点数量计入分母。知识点完成条件默认为 `mastery >= completionThreshold && confidence >= confidenceThreshold`，阈值来自课程策略。

考试资格：目标职业/培训主题要求的课程全部完成，且每门课程完成率至少 90%；资格计算结果需保存 `exam_eligibility_snapshot`，记录课程版本、画像版本、规则版本和计算时间，避免前端自行计算。

接口：

- `GET /api/v1/courses/{courseId}/progress`
- `GET /api/v1/training-goals/{goalId}/exam-eligibility`
- `GET /api/v1/students/me/learning-report?courseId=12`

学习报告展示掌握度变化、证据数量、路径版本变化原因和资源动作，但不得暴露隐藏题、grading key、内部 prompt、其他学生数据或完整模型隐藏状态。

## 12. 原有代码的明确修改清单

### 12.1 必改类

- `CourseLearningPathServiceImpl`：删除“按课程顺序直接插入路径”和“插入 resource item”的实现；课程模块只委派新 `PathPlanningService`。
- `LearningPath`：改为路径会话字段；新增 `LearningPathVersion`。
- `LearningPathItem`：删除 `itemType/resourceId/questionId/assignmentId`，增加版本步骤字段。
- `QbKnowledgePoint`：增加课程、章节、类型、状态、内容版本；不得增加知识点难度字段。
- `QbKnowledgeRelation`：增加课程和图版本，明确边方向。
- `StudentKnowledgeState`：增加课程、算法和交互进度字段。
- `StudentProfileSnapshot`：增加 `profileVersion/courseId/calculatedAt/algorithmVersion/correlationId` 和正式五维 JSON。
- `AttemptServiceImpl` / `AnswerController`：在最终有效提交处调用 `InteractionSubmissionService`，禁止多个入口重复更新画像。
- `TeacherAgentResourceServiceImpl`：保留通用生成能力，拆出学生资源工作流；输入必须绑定 path version、ResourceUnit 和 profile version，审核结果落独立表。
- `QbLearningResource`：不再单挂知识点；发布和版本放 `resource_bundle/resource_item`。

### 12.2 可保留兼容但不能继续扩散的结构

- `qb_tag`、`qb_tag_mastery`、`qb_question_tag`：只用于迁移兼容，所有新业务读写切到 knowledge point。
- `qb_learning_path_snapshot`：只读历史展示，停止新增。
- `review_report_json/model_source_json`：可作展示缓存，权威数据是 `resource_review/resource_source`。
- 系统/个人 LLM provider 和 prompt template 两套表本轮不强制合并，服务层统一抽象。



## 13. 实施

### 数据库与共享字典

在现有 SQL 目录内完成知识点/课程/章节统一、图版本和关系迁移、题目知识关系迁移。完成标准：旧 tag 不再参与新业务计算，Neo4j 能存储并校验 ACTIVE 图，现有目录和模块位置保持不变。

### 交互与画像版本

完成 resource interaction、outbox、知识状态更新日志、课程级五维画像和内部查询。完成标准：同一 interaction 最多更新一次，任意 profile version 可复现。

### 纯知识点路径

完成路径会话/版本拆分、DAG 基线、事件判断和局部重规划。完成标准：所有路径响应不含题目/资源；无实际变化不增版本。

### 个性化资源闭环

完成 ResourceUnit、蓝图、生成、监督、修复、发布、隐藏题隔离和复用策略。完成标准：资源可追溯到 path/profile/graph/policy 版本，审核未通过不能发布。

### 算法增强

完成 DIMKT 显式 mastery 输出、离线校准；数据充分后才逐步启用 P-Agent/PPO 和 S-Agent。每个新算法必须灰度、可回退、保留规则基线。

## 14. 测试

### 数据库

- 全量迁移前后关键表行数、外键孤儿、唯一键冲突、JSON 合法性校验通过。
- 同课程只有一个 ACTIVE graph version 和一个 ACTIVE path version。
- interaction、版本、日志不可被普通删除。

### 图谱

- 自环、环、跨课程边、重复边、禁用节点发布均被拒绝。
- 先修闭包和拓扑排序结果稳定；Neo4j 中的节点、关系与 MySQL 图版本元数据一致。
- 图服务不可用时 Java 基础路径可降级且明确记录 `GRAPH_SERVICE_DEGRADED`。

### 画像与交互

- 无新有效 SUBMITTED interaction 时掌握度不更新。
- 同一 interaction/request 重放不会重复判分、更新画像或发事件。
- 同一合法 resource version 的不同新题可分别更新画像。
- 直接和间接证据日志区分正确；profile version 单调递增。

### 路径

- 路径只含知识点，顺序满足先修约束。
- 普通答题只增加 profile version，不增加 path/resource version。
- 连错 3 次、3 次增益小于 0.01、资源完成、目标变化、主动请求、内容失效均按策略触发评估。
- 候选路径与当前路径相同不创建空版本；完成前缀不被改写。

### 资源

- ResourceUnit 不跨课程、不跳步骤、不改顺序、不重复消费步骤。
- 蓝图使用的画像值与指定 profile version 完全一致。
- 隐藏题在学习接口不可见，grading key 永不下发。
- 可见题、例题、历史隐藏题之间的相似度超过阈值时审核不通过。
- 只有 PUBLISHED bundle 可提交交互；STALE/SUPERSEDED 版本只能查看历史。

### 并发与失败

- 双设备同时提交、浏览器重试和消息重复投递均保持幂等。
- 路径刷新与答案提交竞争时，行锁保证处理进度和 ACTIVE 版本一致。
- 画像、路径任一步失败时事务回滚；资源生成失败不回滚已经确认的答题，但 job 明确 FAILED 并可重试。
